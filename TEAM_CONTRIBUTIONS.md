# Team Contributions

| Teammate | Contributions |
|----------|---------------|
| Teammate A | Implemented core pipeline, embedding (SentenceTransformer), PDF chunking, cosine similarity search (`build_index`, `search`, `extract_pdf_chunks` in `semantic_search_engine.py`) |
| Teammate B (me) | CLI args (argparse), docs (README, ARCHITECTURE, TEAM_CONTRIBUTIONS), top-K flag, data_dir/zip_path/extract_dir handling, .txt loader, zip extraction, min-100-docs validation, error handling (empty query, empty corpus, model download, etc.), summarizer integration (`--summarize`) |

*If git history is available, run `git log --oneline` and `git blame` to refine contributions.*
